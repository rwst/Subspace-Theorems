# cache and other artifacts produced by Lean's Lake build tool
.lake/

# scripts/ is this repository's own gate machinery, adapted from Tau Ceti and tracked. What stays
# ignored is the part of the verbatim copy we have not adapted — local reference only, not
# committed, and nothing tracked may point at it. See scripts/PROVENANCE.md.
/scripts/lean_source.py
/scripts/lint-dot-notation.py
/scripts/lint-dot-notation-baseline.txt
/scripts/lint-baseline.txt
/scripts/lint-nolints-allowlist.txt
/scripts/check-expired-mathlib-shims.py

# TauCeti/ is Tau Ceti's contract and configuration, copied verbatim at its upstream paths, for
# reading only. Nothing tracked may point into it. See TauCeti/PROVENANCE.md.
/TauCeti/

# Literature, kept locally rather than committed
*.pdf
